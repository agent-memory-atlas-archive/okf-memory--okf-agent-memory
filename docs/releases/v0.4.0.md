### 🔐 Foundational Vault Crypto & Sync Engine (v0.4.0)

This release introduces the foundational building blocks for encrypted, remote knowledge synchronization:

- **pkg/vault**: Zero-Knowledge bundle encryption using Argon2id key derivation and AES-256-GCM authenticated envelopes with content-addressed asset trees.
- **pkg/sync**: Low-overhead sync protocol for push/pull reconciliation against blind CAS backends.

**Does this change existing workflows?**
No. `okf-agent-memory` remains 100% local-first. The core library (`pkg/okf`) continues to have **0 external dependencies** and relies exclusively on the Go standard library. Your local Markdown bundles and Git-based workflows work exactly as before. This release lays the protocol foundation for upcoming multi-device sync and the OKF Memory Hub.

**Dependency Policy Clarification:**
To deliver state-of-the-art Argon2id key derivation for zero-knowledge vault synchronization, `pkg/vault` relies on the official Go sub-repository `golang.org/x/crypto`. The project maintains a strict **zero third-party dependency policy**—no external vendor libraries, external frameworks, or CGo dependencies are permitted.

### 🚀 New OKF Hub CLI Commands (`okf hub`)
- **`okf hub init-vault`**: Scaffolds zero-knowledge vault credentials (Vault ID, 128-bit Secret Key Emergency Kit, and `.okf-vault.json`).
- **`okf hub push`**: Client-side encrypted delta sync via CAS (`/blobs/check-missing`) with atomic head pointer updates.
- **`okf hub pull`**: Atomically clones or pulls the latest encrypted remote tree and decrypts locally to disk.
- **`okf hub sync`**: Automated two-way sync with 3-way reconciliation and non-destructive conflict preservation (`<file>.conflict-local.md`).
- **`okf hub serve`**: Built-in blind CAS and atomic head pointer server for self-hosting and zero-dependency local testing.
- **Bearer Token Auth**: Complete support for token-protected hubs via `-auth-token` flag, `OKF_HUB_TOKEN` environment variable, or stored `auth_token` in `.okf-vault.json`.

### Full Changelog
See commits between `v0.3.1...v0.4.0` on [GitHub](https://github.com/okf-memory/okf-agent-memory/compare/v0.3.1...v0.4.0).

